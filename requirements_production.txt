# 生产环境依赖文件
# 基于原有requirements.txt，添加生产环境必需的包

# 原有依赖
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-multipart==0.0.6 
requests==2.31.0
Jinja2==3.1.2
pandas>=1.3.0
openpyxl>=3.0.0
redis>=4.5.0
playwright==1.54.0
pytest==8.4.1
pytest-playwright==0.7.0
pytest-asyncio==0.21.1

# 生产环境额外依赖
gunicorn==21.2.0          # WSGI服务器
python-dotenv==1.0.0      # 环境变量管理
psutil==5.9.6            # 系统监控
aiofiles==23.2.1         # 异步文件操作

# 安全相关
cryptography==41.0.7     # 加密库
passlib==1.7.4           # 密码哈希

# 日志和监控
structlog==23.2.0        # 结构化日志
prometheus-client==0.19.0 # 监控指标

# 数据库相关
alembic==1.12.1          # 数据库迁移
sqlalchemy==2.0.23       # ORM（如果需要）

# 缓存相关
redis-py-cluster==2.1.3  # Redis集群支持

# 开发和调试工具（可选）
ipython==8.17.2          # 交互式Python
rich==13.7.0             # 美化输出






